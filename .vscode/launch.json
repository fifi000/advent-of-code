{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "name": "Debug zig file",
      "request": "launch",
      "program": "${workspaceFolder}\\zig-out\\bin\\debug",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zig build-exe"
    }
  ]
}
